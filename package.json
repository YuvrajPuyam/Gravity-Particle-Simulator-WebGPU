{
  "name": "webgpu-gravity-simulation",
  "version": "1.0.0",
  "description": "gravity particle simulation in WebGPU.",
  "dependencies": {
    "gl-matrix": "^3.4.3"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.6",
    "raw-loader": "^4.0.2",
    "typescript": "^5.0.0",
    "webpack": "^5.58.0",
    "webpack-cli": "^4.9.0",
    "serve": "^14.0.0"
  },
  "keywords": [],
  "scripts": {
    "build": "rmdir /s /q out && mkdir out && npx tsc && npx webpack && type nul > out\\.nojekyll && xcopy dist out\\dist /E /I && copy index.html out\\ && copy styles.css out\\",
    "start": "npx serve out"
  }
}
